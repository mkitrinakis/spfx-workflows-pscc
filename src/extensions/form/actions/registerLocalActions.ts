import { ActionRegistry } from 'workflows-core';

/**
 * Register list-specific (local) form actions here.
 * Global actions are registered automatically by ActionExecutionService.
 */
export function registerLocalActions(): void {
  // Example:
  // ActionRegistry.register(new MyLocalAction());
}
